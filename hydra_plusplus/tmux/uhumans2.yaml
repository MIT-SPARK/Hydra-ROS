---
session_name: uhumans2_{{ robot_name }}
shell_command_before:
  - source /opt/ros/jazzy/setup.{{ shell.extension }}
  - source {{ workspace_path }}
options:
  default-command: {{ shell.cmd }}
windows:
  - window_name: hydra
    focus: true
    panes:
      {% if run_zenoh %}
      - ros2 run rmw_zenoh_cpp rmw_zenohd
      {% endif %}
      - ros2 run ianvs play_rosbag {{ bag_path }}
        --clock
        -d {{ delay }}
        -r {{ rate }}
        --start-offset {{ bag_start }}
        --playback-duration {{ bag_duration }}
      - ros2 launch hydra_plusplus uhumans2_robot.launch.yaml
          exit_after_clock:='true'
          robot_name:={{ robot_name }}
          robot_id:={{ robot_id }}
          object_variant:={{ object_variant }}
          log_path:={{ output_path }}
          start_visualizer:={{ visualize }}
          start_rviz:={{ visualize }}
          start_config_gui:={{ visualize and start_gui }}
        {% if close_session %}
        && tmux kill-session
        {% endif %}
