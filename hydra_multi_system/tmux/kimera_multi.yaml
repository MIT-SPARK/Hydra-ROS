---
session_name: kimera_multi_{{ robot_name }}
shell_command_before:
  - source /opt/ros/jazzy/setup.{{ shell.extension }}
  - source {{ workspace_path }}
options:
  default-command: {{ shell.cmd }}
windows:
  - window_name: {{ robot_name }}_data
    panes:
      {% if run_zenoh %}
      - ros2 run rmw_zenoh_cpp rmw_zenohd
      {% endif %}
      - ros2 launch hydra_multi_system play_kimera_multi.launch.yaml
          robot_name:={{ robot_name }}
          delay:={{ delay }}
          rate:={{ rate }}
          start:={{ bag_start }}
          duration:={{ bag_duration }}
          bag_path:={{ bag_path }}
  - window_name: {{ robot_name }}_hydra
    focus: true
    panes:
      - ros2 launch hydra_multi_system kimera_multi_robot.launch.yaml
          exit_after_clock:='true'
          robot_name:={{ robot_name }}
          robot_id:={{ robot_id }}
          sensor_type:={{ sensor_type }}
          object_variant:={{ object_variant }}
          use_ground_seg:={{ use_ground_seg | lower }}
          ground_seg_sensor_type:={{ ground_seg_sensor_type }}
          log_path:={{ output_path }}
          start_visualizer:={{ visualize }}
          start_rviz:={{ visualize }}
          start_config_gui:={{ visualize and start_gui }}
        {% if close_session %}
        && tmux kill-session
        {% endif %}
